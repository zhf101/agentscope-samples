# `runtime/alias_sandbox/__init__.py` 完全小白讲解

对应文件：`src/alias/runtime/alias_sandbox/__init__.py`

这是 alias_sandbox 包入口，主要导出 `AliasSandbox`。

---

## 作用

1. 提供模块总说明（客户端适配器 + 容器内 box 服务）
2. 暴露 `AliasSandbox` 供上层调用

---

## 一句话总结

它是 alias 沙箱能力对外暴露的统一入口。
