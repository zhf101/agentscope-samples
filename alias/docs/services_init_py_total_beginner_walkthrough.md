# `server/services/__init__.py` 完全小白讲解

对应文件：`src/alias/server/services/__init__.py`

这是 services 包入口文件。

---

## 作用

1. 声明 `services/` 为 Python 包
2. 说明该目录是业务服务层（负责业务编排）

---

## 一句话总结

它是服务层目录的包级注释入口，不直接处理业务请求。
