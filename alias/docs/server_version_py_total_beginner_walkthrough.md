# `server/_version.py` 完全小白逐行讲解

对应文件：`src/alias/server/_version.py`

这个文件只有一个职责：定义项目版本号常量。

---

## 核心内容

```python
__version__ = "0.2.0"
```

常见用途：
- 启动日志打印版本
- 对外接口返回版本
- 打包发布时展示版本

---

## 一句话总结

`_version.py` 是版本信息的单一来源，避免项目里到处写重复版本字符串。
