# `server/exceptions/__init__.py` 完全小白讲解

对应文件：`src/alias/server/exceptions/__init__.py`

这是 exceptions 包入口文件。

---

## 作用

1. 把 `exceptions/` 目录声明为 Python 包
2. 作为统一导入根路径：`alias.server.exceptions.*`
3. 放包级说明注释

---

## 一句话总结

它是包结构文件，不承载具体异常逻辑。
