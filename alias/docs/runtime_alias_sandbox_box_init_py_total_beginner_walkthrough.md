# `runtime/alias_sandbox/box/__init__.py` 完全小白讲解

对应文件：`src/alias/runtime/alias_sandbox/box/__init__.py`

这是沙箱容器应用包入口文件。

---

## 作用

说明该包中的代码运行在 sandbox 容器内部，
负责对外提供容器内工具 API。

---

## 一句话总结

`box/__init__.py` 是容器内应用层的包级说明入口。
