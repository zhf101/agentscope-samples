# `memory_service/service/config/__init__.py` 完全小白讲解

对应文件：`src/alias/memory_service/service/config/__init__.py`

这是 config 包入口文件。

---

## 作用

1. 声明 `config/` 为 Python 包
2. 提供包级说明文档

---

## 一句话总结

它是配置目录的包结构入口，不承载具体配置逻辑。
