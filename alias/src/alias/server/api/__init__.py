# -*- coding: utf-8 -*-
"""
API 包入口（新手教学注释版）。

这个文件本身不放业务逻辑，主要用于把目录声明成 Python 包，
并作为 `alias.server.api.*` 导入路径的根节点。

补充说明（参考 docs/api_init_py_total_beginner_walkthrough.md）：
1) 让 `api/` 目录变成“Python 包”，这样才能被 import。
2) 提供统一的导入根路径：`alias.server.api.*`。
3) 这里适合放包级别的说明文字，但不放具体业务代码。
"""
