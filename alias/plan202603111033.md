# plan202603111033

## 目标与进度
1. 会话级沙箱复用（同用户复用同一 sandbox_id，避免每次新起容器） - 已完成
2. Runtime 释放后回收到容器池（清理 workspace，复用 warm 容器） - 已完成
3. 复用逻辑与并发约束说明（多用户/多会话的可复用范围） - 已完成

## 变更摘要
1. 会话创建时可复用已有沙箱：`src/alias/server/services/conversation_service.py`
2. 复用开关配置项：`src/alias/server/core/config.py` 与 `.env`
3. Runtime `release()` 回收入池与清理：`D:\code\agentscope\agentscope-runtime\src\agentscope_runtime\sandbox\manager\sandbox_manager.py`

## 下一步待办
1. 加入 per-user 并发限制或 sandbox 占用锁
2. 增加复用统计与监控日志
3. 前端提示“同用户多会话共享浏览器”的风险
