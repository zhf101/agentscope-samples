# gdp 后台相对 alias 的删除文件清单（2026-03-09 21:06）

## 对比口径
- 对比目录：`alias/src/alias` 与 `gdp/src/alias`。
- 判定规则：只统计“`alias` 中存在、`gdp` 中不存在”的文件。
- 已排除运行时缓存文件：`__pycache__/`、`*.pyc`。

## 结果概览
- 删除文件总数：`55`。
- 按模块分布：`ds_agent_utils` 27 个、`data_source` 12 个、`dr_agent_utils` 11 个、其他 5 个。
- 本次未发现 `server/*`、`memory_service/*`、`runtime/*` 下的删除文件。

## 删除文件明细（含“文件是什么”）

### 1. 浏览器辅助能力
- `agent/agents/_build_in_helper_browser/_video_understanding.py`：Python 模块，浏览器视频理解辅助能力实现。

### 2. Agent 主入口
- `agent/agents/_data_science_agent.py`：Python 模块，数据科学 Agent 入口/编排逻辑。
- `agent/agents/_deep_research_agent_v2.py`：Python 模块，深度研究 Agent V2 入口/编排逻辑。

### 3. Data Source 能力（数据源分析）
- `agent/agents/data_source/_data_profiler_factory.py`：Python 模块，数据分析器工厂。
- `agent/agents/data_source/_default_config.json`：JSON 配置文件，数据源默认参数配置。
- `agent/agents/data_source/_typing.py`：Python 模块，数据源相关类型定义。
- `agent/agents/data_source/built_in_prompt/_profile_csv_prompt.md`：Markdown 提示词模板，CSV 画像。
- `agent/agents/data_source/built_in_prompt/_profile_image_prompt.md`：Markdown 提示词模板，图片数据画像。
- `agent/agents/data_source/built_in_prompt/_profile_irregular_xlsx_prompt.md`：Markdown 提示词模板，非规则 Excel 画像。
- `agent/agents/data_source/built_in_prompt/_profile_relationdb_prompt.md`：Markdown 提示词模板，关系型数据库画像。
- `agent/agents/data_source/built_in_prompt/_profile_xlsx_prompt.md`：Markdown 提示词模板，Excel 画像。
- `agent/agents/data_source/data_profile.py`：Python 模块，数据画像逻辑。
- `agent/agents/data_source/data_skill.py`：Python 模块，数据源技能封装。
- `agent/agents/data_source/data_source.py`：Python 模块，数据源核心流程。
- `agent/agents/data_source/utils.py`：Python 模块，数据源工具函数。

### 4. DR Agent Utilities（深度研究工具）
- `agent/agents/dr_agent_utils/__init__.py`：Python 包初始化文件。
- `agent/agents/dr_agent_utils/built_in_prompt/prompt_evaluate_hypothesis.md`：Markdown 提示词模板，假设评估。
- `agent/agents/dr_agent_utils/built_in_prompt/prompt_final_report.md`：Markdown 提示词模板，最终报告生成。
- `agent/agents/dr_agent_utils/built_in_prompt/prompt_initialize_hypotheses.md`：Markdown 提示词模板，初始假设生成。
- `agent/agents/dr_agent_utils/built_in_prompt/prompt_markdown_to_html.md`：Markdown 提示词模板，Markdown 转 HTML。
- `agent/agents/dr_agent_utils/deep_research_sys_prompt.py`：Python 模块，深度研究系统提示词构建。
- `agent/agents/dr_agent_utils/deep_research_task.py`：Python 模块，深度研究任务模型/逻辑。
- `agent/agents/dr_agent_utils/deep_research_tree.py`：Python 模块，研究树结构与处理逻辑。
- `agent/agents/dr_agent_utils/deep_research_worker_builder.py`：Python 模块，研究 worker 构建逻辑。
- `agent/agents/dr_agent_utils/deep_research_worker_response.py`：Python 模块，研究 worker 响应处理。
- `agent/agents/dr_agent_utils/visualize_research_tree.py`：Python 模块，研究树可视化逻辑。

### 5. DS Agent Utilities（数据科学工具）
- `agent/agents/ds_agent_utils/__init__.py`：Python 包初始化文件。
- `agent/agents/ds_agent_utils/agent_hook.py`：Python 模块，DS Agent 生命周期 hook。
- `agent/agents/ds_agent_utils/built_in_prompt/_agent_system_workflow_prompt.md`：Markdown 提示词模板，系统工作流。
- `agent/agents/ds_agent_utils/built_in_prompt/_agent_todo_reminder_prompt.md`：Markdown 提示词模板，待办提醒。
- `agent/agents/ds_agent_utils/built_in_prompt/_brief_response_template.md`：Markdown 模板，简版回复格式。
- `agent/agents/ds_agent_utils/built_in_prompt/_detailed_report_template.md`：Markdown 模板，详细报告格式。
- `agent/agents/ds_agent_utils/built_in_prompt/_files_filter_code.txt`：文本模板，文件筛选代码片段。
- `agent/agents/ds_agent_utils/built_in_prompt/_log_to_markdown_prompt.md`：Markdown 提示词模板，日志转 Markdown。
- `agent/agents/ds_agent_utils/built_in_prompt/_markdown_to_html_prompt.md`：Markdown 提示词模板，Markdown 转 HTML。
- `agent/agents/ds_agent_utils/built_in_prompt/_scenario_data_computation_prompt.md`：Markdown 提示词模板，数据计算场景。
- `agent/agents/ds_agent_utils/built_in_prompt/_scenario_data_modeling_prompt.md`：Markdown 提示词模板，数据建模场景。
- `agent/agents/ds_agent_utils/built_in_prompt/_scenario_explorative_data_analysis.md`：Markdown 提示词模板，探索性分析场景。
- `agent/agents/ds_agent_utils/built_in_prompt/_scenario_selected_prompt.md`：Markdown 提示词模板，场景选择。
- `agent/agents/ds_agent_utils/built_in_prompt/_spreadsheet_to_json.md`：Markdown 提示词模板，表格转 JSON。
- `agent/agents/ds_agent_utils/built_in_prompt/_summarize_chart_code.txt`：文本模板，图表代码总结。
- `agent/agents/ds_agent_utils/built_in_prompt/_summary_image_prompt.md`：Markdown 提示词模板，图片总结。
- `agent/agents/ds_agent_utils/built_in_prompt/_tool_todo_list_prompt.yaml`：YAML 提示词模板，工具待办列表。
- `agent/agents/ds_agent_utils/ds_config.py`：Python 模块，DS 配置定义。
- `agent/agents/ds_agent_utils/ds_toolkit.py`：Python 模块，DS 工具集封装。
- `agent/agents/ds_agent_utils/prompt_selector/__init__.py`：Python 包初始化文件。
- `agent/agents/ds_agent_utils/prompt_selector/base_prompt_selector.py`：Python 模块，提示词选择器基类。
- `agent/agents/ds_agent_utils/prompt_selector/llm_prompt_selector.py`：Python 模块，LLM 提示词选择器实现。
- `agent/agents/ds_agent_utils/report_generation.py`：Python 模块，报告生成逻辑。
- `agent/agents/ds_agent_utils/todoWrite.py`：Python 模块，待办写入逻辑。
- `agent/agents/ds_agent_utils/tools/multimodal/image_understanding.py`：Python 模块，多模态图片理解工具。
- `agent/agents/ds_agent_utils/tools/prepare_dataset/clean_messy_spreadsheet.py`：Python 模块，脏表清洗工具。
- `agent/agents/ds_agent_utils/utils.py`：Python 模块，DS 工具函数。

### 6. 其他工具文件
- `agent/tools/improved_tools/multimodal_to_text.py`：Python 模块，多模态内容转文本工具。
- `agent/utils/prepare_data_source.py`：Python 模块，数据源预处理辅助逻辑。

## Python 后端“注释掉代码”排查（gdp 现状）

### 排查范围与方法
- 范围：`gdp/src/alias/**/*.py`（含 `agent`、`server`、`memory_service`、`runtime`）。
- 方法：检索注释行中疑似代码语句（如 `# import`、`# if`、`# await`、`# xxx =`）。
- 结果：命中 `16` 个文件；大部分为调试输出、可选开关、兼容回退入口，未发现大段核心流程被整块注释掉。

### A. 功能开关/兼容回退（对行为有潜在影响）
- `gdp/src/alias/server/api/v1/__init__.py:13`  
  注释了旧路由导入：`# from alias.server.api.v1.chat import router as chat_router`。  
  现状：默认走 `chat_runtime` 路由（运行时实现），保留回退说明。

- `gdp/src/alias/cli.py:86-90`  
  注释了“创建 sandbox 前安装 SIGINT 处理器”的旧写法。  
  现状：在 `sandbox.__enter__()` 后重新安装信号处理器（见 112-121 行），避免 sandbox 内部 handler 覆盖。

- `gdp/src/alias/runtime/alias_sandbox/box/routers/mcp_utils.py:60`  
  注释了 `cwd` 透传：`# cwd=self.config.get("cwd"),  # Disabled`。  
  现状：stdio MCP 子进程不设置工作目录，按默认 cwd 启动。

- `gdp/src/alias/memory_service/service/config/settings.py:79,87`  
  注释了默认 `graph_store` 配置挂载（以及 `MEM0_GRAPH_CONFIG` 字段）。  
  现状：默认以内存 + 向量库为主，图数据库不是默认启用项。

- `gdp/src/alias/memory_service/service/config/settings.py:116-123`  
  注释了本地 path 版 Qdrant 配置样例（`path: tmp/...`）。  
  现状：当前使用 `host/port` 连接 Qdrant 服务。

- `gdp/src/alias/memory_service/service/config/settings.py:146,153`  
  注释了两处 `history_db_path` 旧路径方案。  
  现状：实际使用 `~/.mem0/{collection}.db`。

- `gdp/src/alias/agent/tools/add_qa_tools.py:26-30`  
  注释了 `toolkit.create_tool_group(...qa_mode...)`。  
  现状：QA 工具直接注册，不再单独建 `qa_mode` 组。

- `gdp/src/alias/agent/tools/add_qa_tools.py:106,135`  
  注释了 `group_name="qa_mode"` 归组参数。  
  现状：注册到默认工具集。

- `gdp/src/alias/agent/tools/add_qa_tools.py:80`  
  注释了 `QdrantStore(location=":memory:")`。  
  现状：改为服务端 Qdrant（`host=127.0.0.1, port=6333`）。

- `gdp/src/alias/agent/agents/meta_planner_utils/_worker_manager.py:601`  
  注释了 `structured_model=WorkerResponse`。  
  现状：worker 返回不强制结构化解析，后续通过 `metadata` 再解析。

- `gdp/src/alias/memory_service/user_profiling_memory.py:662`  
  注释了 `extract_facts=True`。  
  现状：`direct_add_profile` 走 `infer=False`，不额外启用事实抽取。

- `gdp/src/alias/memory_service/models/user_profiling.py:127`  
  注释了 `model_config = SettingsConfigDict(env_file=".env")`。  
  现状：`UserProfilingServiceSettings` 未显式绑定 `.env`，依赖环境变量/默认值。

### B. 调试/日志相关（主要影响可观测性，不改核心业务）
- `gdp/src/alias/agent/agents/_browser_agent.py:630,634,778,783,827,831`  
  注释了多处 `await self.print(...)` 流式输出。  
  现状：保留 `logger.info(...)` 记录，减少终端实时打印。

- `gdp/src/alias/agent/utils/llm_call_manager.py:19`  
  注释了 tenacity 的 `before_sleep` 回调。  
  现状：重试前不打印该回调日志。

- `gdp/src/alias/memory_service/profiling_utils/logging_utils.py:25,38-39,49,53`  
  注释了日志文件路径 `print` 与 console handler。  
  现状：仅文件日志 handler，默认不向 stdout 额外输出。

- `gdp/src/alias/memory_service/service/core/task_manager.py:9`  
  注释了 `from loguru import logger`。  
  现状：统一使用 `setup_logging()`。

- `gdp/src/alias/memory_service/memory_base/storage.py:2,8`  
  注释了 `import logging` 与 `logging.getLogger`。  
  现状：同样改为 `setup_logging()`。

- `gdp/src/alias/memory_service/memory_base/base_vec_memory.py:45`  
  注释了 `logger = logger.getLogger(__name__)`。  
  现状：使用上方已初始化 logger。

### C. 模板/说明性注释（非停用逻辑）
- `gdp/src/alias/server/alembic/env.py:35-37,44`  
  Alembic 默认模板注释（`mymodel`、`my_important_option` 示例），属于样板文本。

- `gdp/src/alias/agent/memory/longterm_memory_utils.py:8-11`  
  “class registry”等说明性注释，不是被注释掉的可执行代码。

## 在保留 `general planner` + `browser-use` + `runtime/memory` 前提下的进一步清理建议

### 0. 可立即清理（低风险，不影响功能）
- 清理 Python 运行缓存：`gdp/src/alias/**/__pycache__/`、`gdp/src/alias/**/*.pyc`。  
  当前扫描结果：`__pycache__` 目录 `46` 个、`.pyc` 文件 `346` 个。
- 清理前端安装缓存：`gdp/frontend/.npm-cache/`。  
  当前扫描结果：缓存文件约 `2053` 个。

### 1. 可裁剪的“未接入资源”（低风险，建议先确认）
- `gdp/src/alias/agent/agents/_built_in_skill/data/**/SKILL.md`（5 个文件）  
  在 `gdp/src/alias` 内未检索到引用，当前代码路径未加载这些 SKILL 文本。

### 2. 功能裁剪项（中风险，需产品确认）
- QA 模式相关模块（若确认不需要“AgentScope 问答/RAG/GitHub MCP”可裁掉）：
  - `gdp/src/alias/agent/tools/add_qa_tools.py`
  - `gdp/src/alias/agent/agents/qa_agent_utils/create_rag_file.py`
  - `gdp/src/alias/agent/agents/qa_agent_utils/as_faq_samples.txt`
  - `gdp/src/alias/agent/agents/qa_agent_utils/build_in_prompt/qaagent_base_sys_prompt.md`
- 同步代码调整点（避免残留引用）：
  - `gdp/src/alias/agent/agents/_meta_planner.py` 中 `enter_qa_mode` 工具注册与调用说明文案。
- 影响范围说明：不会影响 `general planner` 的规划执行主链和 `browser-use` 主链，但会移除“专用 QA 模式”能力。

### 3. 不建议当前清理（高风险）
- `gdp/src/alias/runtime/**`、`gdp/src/alias/memory_service/**`、`gdp/src/alias/server/clients/memory_client.py`：属于 runtime/memory 基础能力链路。
- `gdp/src/alias/server/api/v1/share/**`：前端分享页与公开文件预览依赖该路由。
